// ─── Contact ──────────────────────────────────────────────────────────────────
// Primary line (used for sales / PLD / pricing enquiries)
export const WA_NUMBER = "5574181809";
export const WA_DISPLAY = "+52 55 74 1818 09";
export const WA_LINK = `https://wa.me/${WA_NUMBER}`;

// Secondary line (used in webinar / automation pages)
export const WA_NUMBER_2 = "5574181809";
export const WA_DISPLAY_2 = "55 74 1818 09";
export const WA_LINK_2 = `https://wa.me/${WA_NUMBER_2}`;

export const EMAIL_MAIN = "hola@nuvigant.com";
export const EMAIL_CONTACT = "contacto@nuvigant.com";
export const EMAIL_ADMIN = "admin@nuvigant.com";

// ─── URLs ─────────────────────────────────────────────────────────────────────
/** Same-site paths — work on any host (local, preview, production). */
export const URL_SCHEDULE_DEMO = "/schedule-demo";
export const URL_ISR = "https://isr.nuvigant.com";
export const URL_ISR_CALCULATOR = `${URL_ISR}/calculo-isr`;
export const URL_ISR_CONVENIO = `${URL_ISR_CALCULATOR}?show_welcome_message=true`;

// ─── Pricing ──────────────────────────────────────────────────────────────────
/** Software administrativo — monthly base price (MXN, ex. IVA) */
export const PRICE_SOFTWARE_ADMIN = "6,900";

/** Módulo de prevención de lavado — monthly add-on (MXN, ex. IVA) */
export const PRICE_PLD_MODULE = "1,900";

/** ISR Pro — annual licence (MXN, ex. IVA) */
export const PRICE_ISR_PRO = "5,800";

/** Users included in the base software plan */
export const USERS_INCLUDED = 5;

/** Price per additional user above the included limit (MXN, ex. IVA) */
export const PRICE_ADDITIONAL_USER = 300;

// Regularisation service
/** Base price for up to REGULARIZACION_BASE_NAMES names (MXN, ex. IVA) */
export const PRICE_REGULARIZACION_BASE = 2200;

/** Included names in the base regularisation package */
export const REGULARIZACION_BASE_NAMES = 200;

/** Extra cost per name above the base package (MXN, ex. IVA) */
export const PRICE_REGULARIZACION_EXTRA = 1.2;

// ─── Stats / Social proof ─────────────────────────────────────────────────────
export const ISR_USER_COUNT = "6,000";
